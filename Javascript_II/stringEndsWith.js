/*
Complete the solution so that it returns true if the first argument(string) passed in ends with the 2nd argument (also a string).

Examples:

solution('abc', 'bc') // returns true
solution('abc', 'd') // returns false


parameter -- 2 strings, str and ending
returning - true or false statement
example -- 'anti' 'ie' // false
pseudocode --
  endsWith method
  
*/

function solution(str, ending){
    // TODO: complete
   return str.endsWith(ending)
    
  }
  
  /*
  endsWith -- methods to help determine whether string inside of parenthesis ends with that string. if so it is true, if not false 
  
  */